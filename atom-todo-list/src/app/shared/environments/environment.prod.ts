export const environment = {
  production: true,
  apiUrl: 'https://us-central1-atom-todo-normanlunadev.cloudfunctions.net/app/api',
  // Injected at build time by CI (GitHub Actions secret API_SECRET_KEY).
  apiKey: 'REPLACE_AT_BUILD_TIME',
};
