rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All reads and writes go through the Cloud Function API only.
    // Direct client access is denied.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
